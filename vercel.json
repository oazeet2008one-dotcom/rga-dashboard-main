{
  "version": 2,
  "name": "rga-dashboard-frontend",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist/public",
  "installCommand": "cd frontend && npm install",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production"
  }
}
