{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npx prisma generate && npm run build && echo '=== BUILD COMPLETED ===' && pwd && ls -la && echo '=== DIST CONTENTS ===' && ls -la dist/ && echo '=== FINDING JS FILES ===' && find . -name '*.js' -type f && echo '=== CHECKING MAIN.JS ===' && ls -la dist/main.js || echo 'MAIN.JS NOT FOUND' && echo '=== NEST CONFIG ===' && cat nest-cli.json",
    "installCommand": "cd backend && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && npm run start:prod",
    "port": 3000,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}